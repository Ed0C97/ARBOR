# ═══════════════════════════════════════════════════════════════════════════
# A.R.B.O.R. Enterprise — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values.

# ── Application ───────────────────────────────────────────────────────────
APP_ENV=development
APP_DEBUG=true
APP_SECRET_KEY=change-me-to-a-random-string

# ── DATABASE 1: magazine_h182 (READ-ONLY — your existing brands/venues) ──
DATABASE_URL=postgresql+asyncpg://magazine:SKxYmYHxysRWTddSsMy9cUSqipGyR3RM@dpg-d58lpjmr433s73fc1esg-a.oregon-postgres.render.com/magazine_h182
DATABASE_SSL=true

# ── DATABASE 2: arbor_db (READ-WRITE — created by docker-compose) ────────
ARBOR_DATABASE_URL=postgresql+asyncpg://arbor:arbor_password@localhost:5433/arbor_db
ARBOR_DATABASE_SSL=false

# ── Infrastructure (all via docker-compose up -d) ─────────────────────────
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=entities_vectors
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=arbor_dev_password
REDIS_URL=redis://localhost:6379/0
TEMPORAL_HOST=localhost:7233

# ═══════════════════════════════════════════════════════════════════════════
# LLM STACK
# ═══════════════════════════════════════════════════════════════════════════

# ── Google Gemini (text + vision) ─────────────────────────────────────────
GOOGLE_API_KEY=your-gemini-api-key-here
GOOGLE_MODEL=gemini-3-pro-preview

# ── Cohere (embedding + reranking) ────────────────────────────────────────
COHERE_API_KEY=your-cohere-api-key-here
COHERE_EMBEDDING_MODEL=embed-v4.0
COHERE_RERANK_MODEL=rerank-v4.0-pro

# ── LLM Routing ──────────────────────────────────────────────────────────
LLM_PRIMARY_PROVIDER=google
LLM_VISION_PROVIDER=google
LLM_EMBEDDING_PROVIDER=cohere
LLM_FAST_PROVIDER=google
